# Dataset Guide — RoadRishi RouteResilience

This document covers every dataset used in the RoadRishi pipeline, how to obtain it, and which module loads it.

---

## Quick Reference

| Dataset | Role | Resolution | Size | Priority | Loader |
|---|---|---|---|---|---|
| DeepGlobe Road | Pre-train (binary seg) | 0.5 m | ~3.8 GB | 🔴 HIGH | `DeepGlobeDataset` |
| OSM Road Vectors | Ground truth + graph | Vector | ~50 MB | 🔴 HIGH | `osm_graph.py` |
| Resourcesat LISS-IV | Fine-tune (Indian terrain) | 5.8 m | Varies | 🟡 MEDIUM | `LISSIVDataset` |
| Sentinel-2 L2A | Fine-tune (multi-res) | 10 m | ~800 MB/scene | 🟡 MEDIUM | `Sentinel2Dataset` |
| Cartosat-3 | Hackathon evaluation | ~0.3 m | Provided | Placeholder | Inference-only |

---

## Directory Structure

```
data/
├── deepglobe/
│   ├── train/
│   │   ├── <id>_sat.jpg      ← satellite image
│   │   └── <id>_mask.png     ← binary road mask
│   └── valid/
├── lissiv/
│   └── *.tif                 ← GeoTIFF 4-band (Bhuvan Portal)
├── sentinel2/
│   └── *.SAFE/               ← Sentinel-2 scene folders
│       ├── B02.jp2
│       ├── B03.jp2
│       ├── B04.jp2
│       └── B08.jp2
├── osm/
│   ├── bengaluru_drive.graphml   ← auto-generated by osm_graph.py
│   └── ...
└── cartosat/
    └── *.tif                 ← provided at hackathon
```

---

## Dataset 1 — DeepGlobe Road Extraction (PRIMARY)

**Where to get it:**
- Kaggle: https://www.kaggle.com/datasets/balraj98/deepglobe-road-extraction-dataset
- The dataset is already attached when you "Copy & Edit" the training notebook on Kaggle

**Format:** JPG satellite images + PNG binary masks (white = road)

**Download to local (optional):**
```bash
pip install kaggle
kaggle datasets download -d balraj98/deepglobe-road-extraction-dataset -p data/deepglobe --unzip
```

---

## Dataset 2 — OSM Road Vectors (AUTO-FETCHED)

**No manual download needed.** The `osm_graph.py` module fetches this automatically via `osmnx`:

```python
from backend.osm_graph import OSMGraphLoader
loader = OSMGraphLoader()
G = loader.fetch_city_graph("Bengaluru")  # downloads + caches to data/osm/
```

Cached as `data/osm/bengaluru_drive.graphml` after first run.

---

## Dataset 3 — Resourcesat LISS-IV (OPTIONAL fine-tune)

**Where to get it:**
- Register (free): https://bhuvan.nrsc.gov.in/
- Navigate to: Bhuvan Thematic Services → LISS-IV → select Karnataka AOI
- Download as GeoTIFF, place in `data/lissiv/`

**Bands:** Band 2 (Green), Band 3 (Red), Band 4 (NIR) at 5.8 m resolution

---

## Dataset 4 — Sentinel-2 L2A (OPTIONAL fine-tune)

**Where to get it:**
- Register (free): https://dataspace.copernicus.eu/
- Search for Karnataka tiles (UTM zone 43N, tile T43PGR covers Bengaluru)
- Download `.SAFE` folder, place in `data/sentinel2/`

**Bands used:** B02 (Blue), B03 (Green), B04 (Red), B08 (NIR) at 10 m

---

## Dataset 5 — Cartosat-3 (HACKATHON PROVIDED)

- Provided by hackathon organizers at event
- ~0.3 m resolution panchromatic + multispectral
- Place in `data/cartosat/` when received
- Used for final evaluation only (inference-only loader)

---

## Notes

- The training notebook (`training/kaggle_segformer_train.py`) only requires **DeepGlobe** — everything else is optional for improving India-specific accuracy.
- OSM vectors are fetched automatically — no account needed.
- After training on Kaggle, download `roadrishi_finetuned.pth` and place it at `training/checkpoints/roadrishi_finetuned.pth` to enable live inference mode.
