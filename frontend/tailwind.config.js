/**
 * Tailwind v4 Configuration
 *
 * NOTE: Tailwind CSS v4 no longer reads theme extensions from this file.
 * Font families, colors, and other design tokens are defined via @theme
 * in frontend/src/index.css instead.
 *
 * This file is kept for tooling compatibility (e.g. IDE plugins that
 * expect tailwind.config.js to exist), but it has no effect on the build.
 */
export default {}
